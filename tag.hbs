{{!< default}}

<div class="gh-postfeed">
  {{#foreach posts}}
    {{> "post" page=../pagination.page }}
  {{/foreach}}
</div>

{{!-- If JS isn't enabled, fallback to standard pagination --}}
<noscript>
  <div>
    {{pagination}}
  </div>
</noscript>
